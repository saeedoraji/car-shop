export enum ThemeLight {
  PRIMARY,
  SECONDARY,
}

export enum ThemeType {
  INFO,
  WARNING,
  ERROR,
  SUCCESS,
}

export enum ThemeColor {
  DEFAULT,
  ERROR,
  SUCCESS,
}

export const enum ThemeObjectSize {
  SMALL,
  MEDIUM,
  LARGE,
}
